# Optional overrides (defaults are OK).
PROFILES_DIR=profiles
DATA_DIR=data

# Python executable path. If empty, server will try ./python/.venv first, then "py" on Windows, then "python".
PYTHON_PATH=
